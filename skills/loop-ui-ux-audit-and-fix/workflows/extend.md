# Extend

Use for a new capability in an otherwise working system. Define the job, success metric, affected entities/states, entry and exit points, roles, permissions, URL behavior, state matrix, telemetry, and reusable components. Write a design spec, identify backend requirements, implement with isolated ownership, and verify the primary and failure paths. Do not rewrite existing IA without the redesign test.
