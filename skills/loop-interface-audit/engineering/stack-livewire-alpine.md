# Livewire and Alpine

Size Livewire components to one job. Choose `wire:model` modifiers by interaction cost, use targeted loading indicators and stable loop keys, prefer computed properties, and dispatch events over tight parent-child coupling. Use Alpine for purely local state so ordinary UI interactions do not round-trip to the server.
