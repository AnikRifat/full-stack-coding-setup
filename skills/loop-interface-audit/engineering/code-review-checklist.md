# Frontend review checklist

Check: state coverage; semantic tokens; types and exhaustiveness; URL state; keyboard and accessibility; performance; loading and errors; naming; dead code; copy; responsive behavior; and anti-generic tells. A pass means evidence supports every applicable item, not that every item was assumed.
