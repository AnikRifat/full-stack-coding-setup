# React and Inertia

Treat server props as source of truth. Use query strings and partial reloads for filters, shared props for auth/tenant/flash, server validation mapped to fields, deferred props for heavy panels, and stable layouts. Add client caching only when it solves a demonstrated interaction problem without duplicating server state.
