# Inventory

Model SKU, variant, batch, serial, location, movement, adjustment, transfer, stock take, supplier, and reorder point. Every stock number names its semantic meaning; every change is a reasoned movement with immutable history. Prioritize reorder decisions, multi-location clarity, variance approval, batch/expiry behavior, and barcode-first input.
