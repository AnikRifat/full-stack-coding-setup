# Generic domain derivation

When no named domain fits, derive entities from models, APIs, routes, labels, and workflows; derive states from enums, transitions, events, and exception handling; derive primary jobs from the user’s repeated tasks. Record confidence and load named domain context later if evidence emerges. Do not force a product into a generic category merely because it is hybrid.
